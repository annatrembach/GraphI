<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

	<Window.Resources>
		<LinearGradientBrush x:Key="LinearPanelBrush" StartPoint="0,0" EndPoint="1,0">
			<GradientStop Color="Yellow" Offset="0"/>
			<GradientStop Color="Blue" Offset="0.5"/>
			<GradientStop Color="Yellow" Offset="1"/>
		</LinearGradientBrush>

		<Style TargetType="Button">
			<Setter Property="Margin" Value="5"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="BorderBrush" Value="White"/>
		</Style>
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Border Grid.Row="0"
                Background="{StaticResource LinearPanelBrush}"
                BorderBrush="#FFCBBEBE"
                BorderThickness="2"
                CornerRadius="10"
                Margin="5">
			<StackPanel Orientation="Horizontal">
				<Button ToolTip="Відкрити" Command="ApplicationCommands.Open">
					<Image Source="A:\study\materials\3kurs\3.2\Декларативне програмування графічних інтерфейсів\GraphI\images\open.jpg" Width="30" Height="30"/>
				</Button>
				<Button ToolTip="Стерти" Command="ApplicationCommands.Delete">
					<Image Source="A:\study\materials\3kurs\3.2\Декларативне програмування графічних інтерфейсів\GraphI\images\delete.png" Width="30" Height="30"/>
				</Button>
				<Button ToolTip="Копіювати" Command="ApplicationCommands.Copy"
                        CommandTarget="{Binding ElementName=editorTextBox}">
					<Image Source="A:\study\materials\3kurs\3.2\Декларативне програмування графічних інтерфейсів\GraphI\images\copy.png" Width="30" Height="30"/>
				</Button>
				<Button ToolTip="Вставити" Command="ApplicationCommands.Paste"
                        CommandTarget="{Binding ElementName=editorTextBox}">
					<Image Source="A:\study\materials\3kurs\3.2\Декларативне програмування графічних інтерфейсів\GraphI\images\paste.jpg" Width="30" Height="30"/>
				</Button>
			</StackPanel>
		</Border>

		<Border Grid.Row="1"
                Background="{StaticResource LinearPanelBrush}"
                BorderBrush="#FFCBBEBE"
                BorderThickness="2"
                CornerRadius="10"
                Margin="5">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBox x:Name="editorTextBox"
                         Grid.Row="0"
                         Margin="5"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         FontSize="{Binding ElementName=fontSlider, Path=Value}" />

				<DockPanel Grid.Row="1" LastChildFill="False" Margin="5">
					<StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
						<Label Content="Розмір шрифту:" VerticalAlignment="Center"/>
						<Slider Margin="4" MinWidth="100" Name="fontSlider" Maximum="100" Minimum="12"/>
					</StackPanel>

					<Button Content="OK" Command="ApplicationCommands.Save" DockPanel.Dock="Bottom" Style="{x:Null}" Margin="5"/>
				</DockPanel>
			</Grid>
		</Border>
	</Grid>
</Window>
